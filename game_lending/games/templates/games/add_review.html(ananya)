{% extends "base.html" %}

{% block content %}
<style>
    /* Centered container styling */
    .content-wrapper {
        max-width: 600px;
        margin: 40px auto;
        padding: 20px;
        background-color: #FAF7F0; /* Match other page container color */
        border-radius: 8px;
        box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.2);
    }

    /* Page header styling */
    h2 {
        font-size: 24px;
        color: #333;
        text-align: center;
        margin-bottom: 20px;
    }

    /* Form styling */
    form {
        display: flex;
        flex-direction: column;
        gap: 15px;
    }

    label {
        font-weight: bold;
        font-size: 14px;
    }

    input[type="text"], textarea, select {
        width: 100%;
        padding: 10px;
        border: 1px solid #ddd;
        border-radius: 5px;
        box-sizing: border-box;
        font-size: 16px;
        background-color: #F2F2F5; /* Light background for input fields */
    }

    textarea {
        resize: vertical;
        min-height: 80px;
    }

    /* Submit button styling */
    button[type="submit"] {
        width: 100%;
        background-color: #C63D2F; /* Match button color */
        color: white;
        padding: 12px 0;
        font-size: 16px;
        font-weight: bold;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
    }

    button[type="submit"]:hover {
        background-color: #A53025; /* Darker red for hover */
    }

    /* Back link styling */
    .back-link {
        display: block;
        margin-top: 15px;
        text-align: center;
        color: #C63D2F; /* Match button color */
        font-weight: bold;
        text-decoration: none;
        transition: color 0.3s ease;
    }

    .back-link:hover {
        color: #A53025; /* Darker red for hover */
    }
</style>

<div class="content-wrapper">
    <h2>Review {{ game.title }}</h2>
    <form method="post">
        {% csrf_token %}
        {{ form.as_p }}
        <button type="submit">Submit Review</button>
    </form>
    <a href="{% url 'game_detail' game.id %}" class="back-link">Back to Game</a>
</div>
{% endblock %}
